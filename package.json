{
  "scripts": {
    "start:go": "air",
    "start:browser-sync": "browser-sync start --proxy 'localhost:4200' --files 'views/*.html, views/css/*.css, views/js/*.js'",
    "start": "npm-run-all --parallel start:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
